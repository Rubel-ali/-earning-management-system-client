import ProfileCard from "@/components/dashboard/profileCard/profileCard"

export const metadata = {
  title: 'Memorial | Profile',
  description: 'Memorial Profile',
}

export default function ProfilePage() {
  return <ProfileCard />
}
